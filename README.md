# mabox-snapshot
Create a snapshot (iso) of a mabox system.


# todo fix readme

# what you need for qemu spice-app 
# mabox-snapshot
Voor de git readme to be compatible with spice app quickemu
 - spice-vdagent + its spice-vdagentd service — clipboard/resolution/mouse integration when the VM's display is SPICE.
      - qemu-guest-agent + qemu-guest-agent.service — host↔guest communication (graceful shutdown, guest info queries from the host side).
